using BL.Servicer.MapCore;
using System;
using System.Collections.Generic;
using System.Linq;

namespace BL.Servicer.Maps.Mapbox
{
    // this is a placeholder class so that Mapbox has at least one 'real' class in it
    // otherwise this recompiles every build.
    public abstract class Placeholder
    {
    }
}
